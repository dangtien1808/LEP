name: PHP Composer

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: self-hosted

    steps:
      - name: Deploy host
        run: sudo sh /home/data/deploy/deploy.sh
        shell: bash
      - name: Deploy server
        run: /home/bmim-user/run_deploy.sh
        shell: bash
