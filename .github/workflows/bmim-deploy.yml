name: PHP Composer

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: bmim-hosted

    steps:
      - name: Deploy
        run: /home/dangtien/run_deploy.sh
        shell: bash
